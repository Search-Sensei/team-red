/**
 * Provides utility functions that are common across the project.
 */
export class Utilities {
    public static isNullOrUndefined(value: any): boolean {
        return value === null || value === undefined;
    }
}