export interface SearchQueryDefinition {
  filterData: string;
  page: number;
  pageSize: number;
  sort: string;
  profile: string;
  query: string;
  didYouMean: string;
  advancedQuery: string;
}
